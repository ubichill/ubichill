apiVersion: v2
name: video-player
description: Ubichill Video Player Plugin with optional Redis caching
type: application
version: 0.0.1
appVersion: "latest"

keywords:
  - video
  - streaming
  - youtube
  - hls

maintainers:
  - name: Ubichill Team
    email: dev@ubichill.com

# Redis dependency removed - use shared ubichill Redis instance when needed
# dependencies:
#   - name: redis
#     version: "17.11.3"
#     repository: "https://charts.bitnami.com/bitnami"
#     condition: redis.enabled