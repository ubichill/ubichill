---
"@ubichill/sdk": minor
---

mod build/lock/verify CLI を `packages/sdk/cli/` に統合し、`ubichill` パッケージの `bin` として
公開できるようにした。README を実際のAPI（`Ubi.state`/`Ubi.entity`等）に合わせて書き直し、
`engines`/`README.md` 同梱などの公開パッケージ整備を行った。
